# Shared Layer

Cross-tier artifacts that define boundaries between frontend and backend.

- `contracts/order-api.contract.json` — endpoint request/response shape
- `contracts/user-profile-api.contract.json` — user profile endpoint request/response/error shape
- `config/app.example.env` — environment variable naming conventions
