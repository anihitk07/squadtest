# Frontend

React UI that shows order history and order details.

## Commands

```bash
npm install
npm run dev
npm run build
npm run lint
```

Use with backend running on `http://localhost:3001` (or set `VITE_API_BASE_URL`).
